#Задание 44 
#В ячейке ниже представлен код генерирующий DataFrame, которая состоит всего из 1 столбца. Ваша задача перевести его в one hot вид. Сможете ли вы это сделать без get_dummies?
#Статья про one hot вид https://colab.research.google.com/drive/1qKamnDiRmpRZkpiqWPkunBdAhmzhMcGz?usp=sharing

import pandas as pd

# Исходный код
import random
lst = ['robot'] * 10
lst += ['human'] * 10
random.shuffle(lst)
data = pd.DataFrame({'whoAmI': lst})

unique_values = data['whoAmI'].unique() # создаем список уникальных значений

one_hot_data = pd.DataFrame() # создаем пустой DataFrame для one-hot кодирования

# заполняем DataFrame one-hot значениями
for value in unique_values:
    one_hot_data[value] = (data['whoAmI'] == value).astype(int)

data = pd.concat([data, one_hot_data], axis=1) # соединяем исходный DataFrame и one-hot DataFrame

data.head()# вывод первых строк нового DataFrame
